---
title: Thread item actions
description: Configure feedback, retry, and share actions on assistant messages.
---

Control which actions show on assistant messages: feedback and retry.

```jsx
const options: Partial<ChatKitOptions> = {
  threadItemActions: {
    feedback: true,
    retry: true,
  },
};
```
